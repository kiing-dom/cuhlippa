# Cuhlippa Application Version Information
app.version=1.0.1
app.build.date=@timestamp@
app.name=Cuhlippa Clipboard Synchronization
app.description=Enterprise-grade clipboard synchronization with automatic device discovery

# Build Information (filled by Maven)
build.timestamp=${maven.build.timestamp}
build.commit=${git.commit.id.abbrev}
build.branch=${git.branch}
